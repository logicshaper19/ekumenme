# ChromaDB Authentication File
# Format: username:password_hash
# Generate with: htpasswd -c htpasswd admin
# Or use: echo "admin:$(openssl passwd -apr1 your-password)" >> htpasswd

# Example (replace with your actual password hash):
# admin:$apr1$salt$hash
